[package]
name = "cuelight-loader"
description = "Load cuelight shows from disk or memory: show folders, PNG images, bitmap fonts, driver scripts"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
keywords = ["multimedia", "animation", "assets", "loader"]
categories = ["multimedia", "rendering"]

[dependencies]
clap = { version = "4", features = ["derive"], optional = true }
cuelight = { path = "../cuelight", version = "0.0.1", default-features = false }
# Decoding only, for sound lengths: no device is opened, so `live` is off.
cuelight-audio = { path = "../cuelight-audio", version = "0.0.1", default-features = false, optional = true }
# Measuring only, for video lengths: reads the header, decodes nothing.
cuelight-video = { path = "../cuelight-video", version = "0.0.1", optional = true }
png = { version = "0.18", optional = true }
schemars = { version = "1", optional = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
# Text becomes paths through the show's own fonts, so the feature that
# reads the machine's is off.
usvg = { version = "0.48", default-features = false, features = ["text"], optional = true }
zip = { version = "8", default-features = false, features = ["deflate-flate2-zlib-rs"], optional = true }

[dev-dependencies]
png = "0.18"
zip = { version = "8", default-features = false, features = ["deflate-flate2-zlib-rs"] }

[features]
default = ["png", "outline-fonts", "svg", "pack"]
# Convert .svg files under assets/ into vector artwork (paths with fills
# and strokes; text becomes paths through the show's own fonts).
svg = ["dep:usvg"]
# Read and write packed shows (.cuelight, a zip of a show folder).
pack = ["dep:zip"]
# Image decoders. Hosts that decode images themselves (a browser, a game
# engine) can turn them off and register pixels with Engine::set_image.
png = ["dep:png"]
# Register .ttf / .otf files as outline fonts (pulls the font metrics code
# into the engine). Without it such files are reported as skipped.
outline-fonts = ["cuelight/outline-fonts"]
# The `cuelight-render` tool: frames at chosen times, and what a show
# fired and when. Pulls in the renderer, and with it wgpu.
# Generate the JSON Schema for driver files.
schema = ["dep:schemars", "cuelight/schema"]
render-cli = ["dep:clap", "png", "cuelight/render", "dep:cuelight-audio", "dep:cuelight-video"]

[[bin]]
name = "cuelight-pack"
required-features = ["pack"]

[[bin]]
name = "cuelight-render"
required-features = ["render-cli"]
