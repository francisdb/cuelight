{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Show",
  "description": "A declarative show description: what a `.json` show file deserializes into.",
  "type": "object",
  "properties": {
    "background": {
      "description": "Background color, `#RRGGBB` or `#RRGGBBAA`.",
      "type": "string",
      "default": "#000000"
    },
    "fonts": {
      "description": "Named text styles for text layers: a bitmap font plus colors.",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/FontStyle"
      },
      "default": {}
    },
    "format": {
      "description": "Show format version, see [`FORMAT`]. A show without it is format 1.",
      "type": "integer",
      "format": "uint32",
      "default": 1,
      "minimum": 0
    },
    "layers": {
      "description": "Layers that are always present, painted behind the active scene.",
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/Layer"
      }
    },
    "name": {
      "type": "string"
    },
    "output": {
      "description": "How rendered colors reach the display: full color by default, or\nquantized luminance tinted in one color (DMD style). Scenes can\noverride it.",
      "$ref": "#/$defs/Output",
      "default": {
        "mode": null,
        "scaling": null,
        "tint": null
      }
    },
    "scenes": {
      "description": "Switchable views; exactly one is active at a time (the first one\nwhen the show loads), entered by firing its trigger.",
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/Scene"
      }
    },
    "size": {
      "description": "Logical canvas size in pixels `[width, height]`. Hosts scale the\nrendered texture; content is authored against this space.\nCoordinates have their origin at the top-left corner: x grows\nright, y grows down.",
      "type": "array",
      "items": {
        "type": "integer",
        "format": "uint32",
        "minimum": 0
      },
      "maxItems": 2,
      "minItems": 2
    },
    "values": {
      "description": "Values the show animates itself, read by bindings the way a\nvariable is. For motion that belongs to the content rather than\nto whatever is driving it.",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/ShowValue"
      },
      "default": {}
    },
    "variables": {
      "description": "Declared variables and their initial values.",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/Value"
      },
      "default": {}
    }
  },
  "required": [
    "name",
    "size"
  ],
  "$defs": {
    "Align": {
      "description": "Where content sits in a box: one of nine positions.",
      "type": "string",
      "enum": [
        "top_left",
        "top",
        "top_right",
        "left",
        "center",
        "right",
        "bottom_left",
        "bottom",
        "bottom_right"
      ]
    },
    "Binding": {
      "description": "A permanent wiring of a property to a variable, evaluated every frame.\n\nNumeric properties take `variable * scale + offset`. The `text`\nproperty takes the variable as text: numbers get `scale`/`offset`\napplied, then `format`. The `font` property takes the variable as a\nfont style name.\n\nWith `map`, the variable's value (as text: `1`, `2.5`, `true`, ...)\nis looked up first and the mapped value, or `default` when it is not\nlisted, takes the variable's place. Without either, the binding does\nnot apply and the property keeps its base value. The order is:\n`debounce`, `map`, `threshold`, `scale` and `offset`, `transition`.",
      "type": "object",
      "properties": {
        "curve": {
          "description": "Bend the value against the input instead of scaling it straight.\n\nKeys are a track's, with the input value where a track has time,\nand the same easings between them: below the first key it holds\nthe first value, above the last it holds the last. A lamp whose\nglow wants a gamma curve, a tachometer compressed at the low end,\na loudness in decibels rather than a linear gain.\n\nThis shapes value against input; a [`Transition`]'s `ease` shapes\na change over time. A binding can have both, and they do different\nthings.\n\nIt applies after `map` and before `scale` and `offset`, so the\ncurve is written in the variable's own units and `scale` stays the\nlast change of unit. It cannot be combined with `threshold`, which\nis the same job done crudely: a `step` ease says it as a curve.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Key"
          }
        },
        "debounce": {
          "description": "Seconds a new value has to hold before it reaches the property;\nchanges shorter than that (a strobing lamp, a bouncing switch)\nnever show. When a show loads or a scene is entered the value\napplies at once.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "decimals": {
          "description": "How many decimal places a number shows, when it becomes text.\n\nWithout it a number prints as short as it can, so a value a\ntimeline is moving reads `1.4833333333333334` between its keys.\nWith it the number is rounded to that many places and always\nshows them: `1.5` with two is `1.50`.\n\nApplies after `scale` and `offset`, as the rest of formatting\ndoes, and alongside `format`.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "default": {
          "description": "Value for variable values `map` does not list.",
          "anyOf": [
            {
              "$ref": "#/$defs/Value"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "format": {
          "description": "How a number becomes text (text bindings only).",
          "$ref": "#/$defs/NumberFormat",
          "default": "plain"
        },
        "map": {
          "description": "Replace the variable's value by looking it up here.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "$ref": "#/$defs/Value"
          },
          "default": null
        },
        "offset": {
          "type": "number",
          "format": "double",
          "default": 0.0
        },
        "property": {
          "$ref": "#/$defs/Property"
        },
        "scale": {
          "type": "number",
          "format": "double",
          "default": 1.0
        },
        "threshold": {
          "description": "A level: the value becomes 1 at or above it and 0 below, before\n`scale` and `offset` apply. A lamp that lights when a brightness\npasses a half, a `visible` that follows a level.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "transition": {
          "description": "Ease toward a new value instead of jumping to it.",
          "anyOf": [
            {
              "$ref": "#/$defs/Transition"
            },
            {
              "type": "null"
            }
          ]
        },
        "variable": {
          "type": "string"
        }
      },
      "required": [
        "property",
        "variable"
      ]
    },
    "Blend": {
      "description": "How a layer's colors combine with the colors already beneath it.\nOpacity applies on top of the blend, as usual.",
      "oneOf": [
        {
          "description": "Paint over: the layer covers what is beneath.",
          "type": "string",
          "const": "normal"
        },
        {
          "description": "Sum the colors: light that adds to the picture, as a lamp behind\nart does. Overlapping glows brighten each other; white saturates.",
          "type": "string",
          "const": "add"
        },
        {
          "description": "`1 - (1 - a)(1 - b)`: light that adds but never saturates, softer\nthan `add`.",
          "type": "string",
          "const": "screen"
        },
        {
          "description": "Product of the colors: a coloured shape darkens and tints what is\nbeneath, as a gel over a lamp does; white leaves it alone.",
          "type": "string",
          "const": "multiply"
        }
      ]
    },
    "Border": {
      "description": "A border of `width` pixels drawn outside every glyph.",
      "type": "object",
      "properties": {
        "color": {
          "description": "`#RRGGBB`",
          "type": "string"
        },
        "width": {
          "description": "Outline width in pixels.",
          "type": "integer",
          "format": "uint32",
          "default": 1,
          "minimum": 0
        }
      },
      "required": [
        "color"
      ]
    },
    "Choice": {
      "description": "An asset name, or a list of names to pick between; see `pick`.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      ]
    },
    "DigitDisplay": {
      "description": "How the cells of a digit row are drawn.",
      "oneOf": [
        {
          "description": "A segment display: lit segments in `fill`, and the dark ones in\n`unlit` when given. A `.` or `,` lights the dot of the cell before\nit instead of taking a cell. Characters the style cannot show stay\ndark.",
          "type": "object",
          "properties": {
            "segments": {
              "type": "object",
              "properties": {
                "fill": {
                  "type": "string"
                },
                "glow": {
                  "description": "A halo around lit segments, in their own colour. Unlit ones\nnever glow.",
                  "anyOf": [
                    {
                      "$ref": "#/$defs/Glow"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "slant": {
                  "description": "Degrees the cells lean, as a shear rather than a rotation, so\nthe baseline stays level. Most real displays lean about ten,\nand 45 is as far as it goes. Positive leans the tops to the\nright.",
                  "type": "number",
                  "format": "double",
                  "default": 0.0
                },
                "style": {
                  "$ref": "#/$defs/SegmentStyle"
                },
                "thickness": {
                  "description": "Segment width as a share of the cell's shorter side, 0.1 by\ndefault and 0.2 at most. The gaps between segments follow it,\nso a fat display stays legible instead of running together,\nand past that cap the bars would meet.",
                  "type": [
                    "number",
                    "null"
                  ],
                  "format": "double"
                },
                "unlit": {
                  "type": [
                    "string",
                    "null"
                  ],
                  "default": null
                }
              },
              "required": [
                "style",
                "fill"
              ]
            }
          },
          "additionalProperties": false,
          "required": [
            "segments"
          ]
        },
        {
          "description": "Cells that roll through a ring of characters, drawn in a font.",
          "type": "object",
          "properties": {
            "reel": {
              "$ref": "#/$defs/Reel"
            }
          },
          "additionalProperties": false,
          "required": [
            "reel"
          ]
        }
      ]
    },
    "Direction": {
      "description": "Which way round a wrapped [`Transition`] goes.",
      "oneOf": [
        {
          "description": "Whichever way is shorter; forward on a tie.",
          "type": "string",
          "const": "shortest"
        },
        {
          "description": "Always toward higher values: 9 to 0 rolls on through the wrap.",
          "type": "string",
          "const": "forward"
        },
        {
          "description": "Always toward lower values.",
          "type": "string",
          "const": "backward"
        }
      ]
    },
    "DotShape": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "round"
          ]
        },
        {
          "description": "Square dots with gaps: an LED matrix.",
          "type": "string",
          "const": "square"
        }
      ]
    },
    "Dots": {
      "description": "The dot matrix look: canvas pixels shown as separate dots on black.\nBelow three surface pixels per dot there is no room for it and the frame\nis shown plain.",
      "type": "object",
      "properties": {
        "glow": {
          "description": "How much lit dots bleed into the dark around them, 0 (not at all,\nthe default) to 1.",
          "type": "number",
          "format": "double",
          "default": 0.0
        },
        "shape": {
          "$ref": "#/$defs/DotShape",
          "default": "round"
        },
        "size": {
          "description": "Dot diameter as a share of the pixel pitch, above 0 up to 1.",
          "type": "number",
          "format": "double",
          "default": 0.8
        },
        "unlit": {
          "description": "Color of a dot that is off, `#RRGGBB`: the faint dots of a real\npanel. Dots never get darker than this. None by default.",
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "Duck": {
      "description": "Step this layer back while something on another bus is sounding.\n\nA bed under clips that speak over it has to get out of the way and\ncome back, which is the ordinary arrangement whenever there is music\nunder anything that talks. `gain` is bindable and a transition can\nease it, but nothing in a show can see that something else is\nsounding, so without this a host has to watch the engine's voices and\nfeed a variable back, putting show structure outside the show.\n\nIt multiplies like every other gain, so it composes with bindings and\nwith the gain of the groups above rather than fighting them.",
      "type": "object",
      "properties": {
        "attack": {
          "description": "Seconds to go down. 0 (the default) drops at once, which is what\nmakes room in time for the first word.",
          "type": "number",
          "format": "double",
          "default": 0.0
        },
        "release": {
          "description": "Seconds to come back up once the bus falls silent. The part that\nhas to be smooth.",
          "type": "number",
          "format": "double",
          "default": 0.0
        },
        "to": {
          "description": "Gain multiplier while that bus sounds. 0.1 is a tenth.",
          "type": "number",
          "format": "double"
        },
        "under": {
          "description": "The bus to listen to. Anything sounding on it ducks this layer;\nthe layer's own plays never do.",
          "type": "string"
        }
      },
      "required": [
        "under",
        "to"
      ]
    },
    "Easing": {
      "description": "Easing applied to the segment that ends at the keyframe carrying it.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "linear",
            "quad_in",
            "quad_out",
            "quad_in_out",
            "cubic_in",
            "cubic_out",
            "cubic_in_out",
            "step",
            "back_in_out"
          ]
        },
        {
          "description": "Pulls back below the start before going.",
          "type": "string",
          "const": "back_in"
        },
        {
          "description": "Overshoots the end and comes back to it.",
          "type": "string",
          "const": "back_out"
        },
        {
          "description": "Arrives fast, then swings around the end a few times, settling.",
          "type": "string",
          "const": "elastic_out"
        },
        {
          "description": "Hits the end and bounces off it a few times, never passing it.",
          "type": "string",
          "const": "bounce_out"
        }
      ]
    },
    "Fill": {
      "description": "What a shape is filled with: one color, or a gradient.\n\nUntagged, so `\"fill\": \"#FF0000\"` keeps working and\n`\"fill\": { \"radial\": { ... } }` is the other one.",
      "anyOf": [
        {
          "description": "`#RRGGBB` or `#RRGGBBAA`.",
          "type": "string"
        },
        {
          "$ref": "#/$defs/Gradient"
        }
      ]
    },
    "FontStyle": {
      "description": "A text style: a bitmap font the host registered, tinted and optionally\noutlined.",
      "type": "object",
      "properties": {
        "border": {
          "anyOf": [
            {
              "$ref": "#/$defs/Border"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "color": {
          "description": "Text color, `#RRGGBB`. For bitmap fonts it multiplies the glyph\ncolors, so white keeps the font's own.",
          "type": "string",
          "default": "#FFFFFF"
        },
        "file": {
          "description": "Name of the font as registered by the host, by convention the font\nfile's stem: a bitmap font ([`Engine::set_font`](crate::Engine::set_font))\nor an outline font (`Engine::set_outline_font`). Which kind it is\ndecides how the text is drawn; the layers using the style do not\nchange.",
          "type": "string"
        },
        "shadow": {
          "description": "A copy of the text drawn behind it, offset.",
          "anyOf": [
            {
              "$ref": "#/$defs/Shadow"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "size": {
          "description": "Em size in canvas pixels. Required for outline fonts; not allowed\nfor bitmap fonts, which have one fixed size.",
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "default": null
        }
      },
      "required": [
        "file"
      ]
    },
    "Glow": {
      "description": "A halo around the lit segments of a display.\n\nGas-discharge and fluorescent displays spill light around every lit\nsegment, and a panel recreated without it looks wrong however right\nthe digits are.\n\nDrawn as the segment again, a few times, each wider and fainter than\nthe last: a halo rather than a true blur, which nothing on the GPU can\ngive us yet (see the note on text shadows). At the sizes a display is\ndrawn it reads the same.",
      "type": "object",
      "properties": {
        "size": {
          "description": "How far it reaches beyond the segment, as a share of the cell's\nwidth. About 0.05 is a rim and about 0.25 is as far as it carries,\nwhere the halos of neighbouring digits join.",
          "type": "number",
          "format": "double"
        },
        "strength": {
          "description": "How strong it is where it leaves the segment, 0 to 1.",
          "type": "number",
          "format": "double",
          "default": 0.5
        }
      },
      "required": [
        "size"
      ]
    },
    "Gradient": {
      "description": "A smooth run of colors across a shape, in the shape's own space, so it\ntravels with whatever moves the shape.\n\nGlows, vignettes, the shading that makes a drum look round and the\nsheen on glass are all gradients, and carrying them as small raster\nimages means the same workaround in every show and blurring whenever\none is scaled up.",
      "oneOf": [
        {
          "description": "Runs along the line from `from` to `to`, and holds its end colors\nbeyond either end.",
          "type": "object",
          "properties": {
            "linear": {
              "type": "object",
              "properties": {
                "from": {
                  "type": "array",
                  "items": {
                    "type": "number",
                    "format": "double"
                  },
                  "maxItems": 2,
                  "minItems": 2
                },
                "stops": {
                  "type": "array",
                  "items": {
                    "$ref": "#/$defs/Stop"
                  }
                },
                "to": {
                  "type": "array",
                  "items": {
                    "type": "number",
                    "format": "double"
                  },
                  "maxItems": 2,
                  "minItems": 2
                }
              },
              "required": [
                "from",
                "to",
                "stops"
              ]
            }
          },
          "additionalProperties": false,
          "required": [
            "linear"
          ]
        },
        {
          "description": "Runs out from `center` to `radius`, and holds its last color\nbeyond it.",
          "type": "object",
          "properties": {
            "radial": {
              "type": "object",
              "properties": {
                "center": {
                  "type": "array",
                  "items": {
                    "type": "number",
                    "format": "double"
                  },
                  "maxItems": 2,
                  "minItems": 2
                },
                "radius": {
                  "type": "number",
                  "format": "double"
                },
                "stops": {
                  "type": "array",
                  "items": {
                    "$ref": "#/$defs/Stop"
                  }
                }
              },
              "required": [
                "center",
                "radius",
                "stops"
              ]
            }
          },
          "additionalProperties": false,
          "required": [
            "radial"
          ]
        }
      ]
    },
    "Justify": {
      "description": "Which end of a digit row its text sits against.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "left"
          ]
        },
        {
          "description": "As scores are shown: the last character in the last cell.",
          "type": "string",
          "const": "right"
        }
      ]
    },
    "Key": {
      "description": "A keyframe: at time `t` (seconds) the property reaches value `v`,\napproached with `ease` from the previous key.",
      "type": "object",
      "properties": {
        "ease": {
          "$ref": "#/$defs/Easing",
          "default": "linear"
        },
        "t": {
          "type": "number",
          "format": "double"
        },
        "v": {
          "type": "number",
          "format": "double"
        }
      },
      "required": [
        "t",
        "v"
      ]
    },
    "Layer": {
      "description": "One node in the show tree.",
      "type": "object",
      "properties": {
        "anchor": {
          "description": "Which point of the layer's content box sits at its x/y. Without an\nanchor, images put their top-left corner there and shapes their\nlocal origin. Not supported on groups.",
          "anyOf": [
            {
              "$ref": "#/$defs/Align"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "bindings": {
          "description": "Live property bindings: `property = variable * scale + offset`.",
          "type": "array",
          "default": [],
          "items": {
            "$ref": "#/$defs/Binding"
          }
        },
        "blend": {
          "description": "How the layer combines with what is painted beneath it; a group\nblends its children as one picture.",
          "$ref": "#/$defs/Blend",
          "default": "normal"
        },
        "name": {
          "type": "string"
        },
        "opacity": {
          "description": "Opacity in [0, 1], multiplied down the tree.",
          "type": "number",
          "format": "double",
          "default": 1.0
        },
        "overflow": {
          "description": "Let this layer (and a group's subtree) draw past the canvas.\n\nA show is authored against a fixed canvas and a host fits that\ncanvas into whatever surface it has, painting the area around it\nwith the show's `background`. That is the right default: it keeps\nthe canvas meaning exactly what it means for layout. It is the\nwrong answer for a backdrop whose job is to reach the edges, which\non a wider display sits in two bars of flat colour with nothing\nthe show can say about them.\n\nMarked here, only the clip changes: coordinates are still authored\nagainst the canvas, which stays a safe area for everything placed\nexactly. A show cannot know how far it will be asked to stretch,\nso anything that bleeds has to be drawn generously.\n\nIt does nothing where the frame *is* the canvas: an offscreen\nrender, `pixel_perfect` scaling, a `dots` pass, or an output mode\nother than `rgb`, all of which draw the canvas at its own size\nfirst. There is no area outside a dot matrix to reach into.",
          "type": "boolean",
          "default": false
        },
        "rotation": {
          "description": "Rotation in degrees, clockwise on the canvas, around the layer's\nx/y origin or its anchor point. A group turns its whole subtree.",
          "type": "number",
          "format": "double",
          "default": 0.0
        },
        "scale": {
          "description": "Uniform scale of the layer around its x/y origin (shape coordinates\nand sizes, image destination size), or around its anchor point.\nA group's scale applies to its whole subtree.",
          "type": "number",
          "format": "double",
          "default": 1.0
        },
        "scale_x": {
          "description": "Scale along x and y on top of `scale`, for stretching and flips\n(negative values mirror). Inherited like `scale`.",
          "type": "number",
          "format": "double",
          "default": 1.0
        },
        "scale_y": {
          "type": "number",
          "format": "double",
          "default": 1.0
        },
        "timelines": {
          "description": "Keyframed animations on this layer's properties.",
          "type": "array",
          "default": [],
          "items": {
            "$ref": "#/$defs/Timeline"
          }
        },
        "visible": {
          "type": "boolean",
          "default": true
        },
        "x": {
          "description": "Translation applied to this layer (and its subtree, for groups),\nin canvas coordinates (origin top-left, y down).",
          "type": "number",
          "format": "double",
          "default": 0.0
        },
        "y": {
          "type": "number",
          "format": "double",
          "default": 0.0
        }
      },
      "oneOf": [
        {
          "description": "A container: children are positioned relative to the group and\npainted in order. With `clip`, children only show inside that\nshape, given in the group's local space like a shape layer's.",
          "type": "object",
          "properties": {
            "children": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Layer"
              }
            },
            "clip": {
              "anyOf": [
                {
                  "$ref": "#/$defs/Shape"
                },
                {
                  "type": "null"
                }
              ],
              "default": null
            },
            "gain": {
              "description": "Loudness of the sounds in the subtree, multiplied down the tree\nlike opacity.",
              "type": "number",
              "format": "double",
              "default": 1.0
            },
            "type": {
              "type": "string",
              "const": "group"
            }
          },
          "required": [
            "type",
            "children"
          ]
        },
        {
          "description": "A vector shape filled with `fill`, and outlined by `stroke` when\ngiven. The fill is a color, or a gradient that stays smooth at any\nsize where a show would otherwise carry a small image of one.",
          "type": "object",
          "properties": {
            "fill": {
              "$ref": "#/$defs/Fill"
            },
            "shape": {
              "$ref": "#/$defs/Shape"
            },
            "stroke": {
              "anyOf": [
                {
                  "$ref": "#/$defs/Stroke"
                },
                {
                  "type": "null"
                }
              ]
            },
            "type": {
              "type": "string",
              "const": "shape"
            }
          },
          "required": [
            "type",
            "shape",
            "fill"
          ]
        },
        {
          "description": "A host-provided raster image, registered under `image` via\n[`Engine::set_image`](crate::Engine::set_image). Drawn with its\ntop-left corner at the layer's x/y unless the layer has an\n`anchor`; not yet registered images are skipped.",
          "type": "object",
          "properties": {
            "frame": {
              "description": "Base cell index for sheets (row-major, 0 is the top-left cell).",
              "type": "number",
              "format": "double",
              "default": 0.0
            },
            "image": {
              "type": "string"
            },
            "repeat": {
              "description": "Tile the image across `size` instead of stretching to it.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Tile"
                },
                {
                  "type": "null"
                }
              ]
            },
            "sheet": {
              "description": "Treat the image as a grid of equally sized cells and draw one:\nthe one the `frame` property selects.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Sheet"
                },
                {
                  "type": "null"
                }
              ],
              "default": null
            },
            "size": {
              "description": "Destination size `[width, height]`; the image's natural size\n(one cell's size with a `sheet`) when omitted.",
              "type": [
                "array",
                "null"
              ],
              "default": null,
              "items": {
                "type": "number",
                "format": "double"
              },
              "maxItems": 2,
              "minItems": 2
            },
            "tint": {
              "description": "Color the image is multiplied by, `#RRGGBB` or `#RRGGBBAA`:\nwhite leaves it alone, a color stains it (a lamp behind white\nart, a worn look, one sprite in several colors).",
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "type": "string",
              "const": "image"
            }
          },
          "required": [
            "type",
            "image"
          ]
        },
        {
          "description": "Text in a bitmap font style from the show's `fonts`. With `size` the\ntext is aligned inside that box (its top-left corner at the layer's\nx/y); without, the box is the text's own size. Multi-line text\n(`\\n`) aligns each line on its own. Skipped while the style's font\nis not registered.",
          "type": "object",
          "properties": {
            "align": {
              "$ref": "#/$defs/Align",
              "default": "center"
            },
            "font": {
              "type": "string"
            },
            "size": {
              "type": [
                "array",
                "null"
              ],
              "default": null,
              "items": {
                "type": "number",
                "format": "double"
              },
              "maxItems": 2,
              "minItems": 2
            },
            "text": {
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "text"
            }
          },
          "required": [
            "type",
            "text",
            "font"
          ]
        },
        {
          "description": "Vector artwork the host registered under `vector`\n([`Engine::set_vector`](crate::Engine::set_vector), the loader does\nit for `assets/*.svg`), drawn like an image: its top-left corner at\nthe layer's x/y (or by `anchor`), at its natural size or scaled\ninto `size`. Skipped while not registered.",
          "type": "object",
          "properties": {
            "size": {
              "description": "Destination size `[width, height]`; the artwork's own size\nwhen omitted.",
              "type": [
                "array",
                "null"
              ],
              "default": null,
              "items": {
                "type": "number",
                "format": "double"
              },
              "maxItems": 2,
              "minItems": 2
            },
            "type": {
              "type": "string",
              "const": "vector"
            },
            "vector": {
              "type": "string"
            }
          },
          "required": [
            "type",
            "vector"
          ]
        },
        {
          "description": "A row of `digits` equal cells across `size` `[width, height]`\n(top-left at the layer's x/y) showing `text`, one character per\ncell; how a cell is drawn is up to `display`. Text longer than the\nrow is cut at the far side of `justify`.",
          "type": "object",
          "properties": {
            "digits": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "display": {
              "$ref": "#/$defs/DigitDisplay"
            },
            "justify": {
              "$ref": "#/$defs/Justify",
              "default": "left"
            },
            "size": {
              "type": "array",
              "items": {
                "type": "number",
                "format": "double"
              },
              "maxItems": 2,
              "minItems": 2
            },
            "text": {
              "type": "string",
              "default": ""
            },
            "type": {
              "type": "string",
              "const": "digits"
            }
          },
          "required": [
            "type",
            "digits",
            "size",
            "display"
          ]
        },
        {
          "description": "A video the host registered under `video` with its duration and\nsize ([`Engine::set_video`](crate::Engine::set_video)), played the\nway a sound is: on `trigger` or at load with `autoplay`, after\n`delay`, looping or repeating, firing `on_end`. The engine decodes\nnothing: it reports what should be showing and at which position\n([`Engine::videos`](crate::Engine::videos)), and draws whatever\nframe the host last registered as an image under the video's name.",
          "type": "object",
          "properties": {
            "autoplay": {
              "description": "Play when the show loads or the scene is entered.",
              "type": "boolean",
              "default": false
            },
            "bus": {
              "description": "Name of the bus the clip's sound plays through; hosts route\nbuses to outputs. [`MAIN_BUS`] when omitted, so every sound is\non one.",
              "type": [
                "string",
                "null"
              ]
            },
            "delay": {
              "description": "Seconds between the trigger and the first frame.",
              "type": "number",
              "format": "double",
              "default": 0.0
            },
            "duck": {
              "description": "Step back while something else is sounding; see [`Duck`]. A\nclip with a soundtrack wants this as much as a sound does.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Duck"
                },
                {
                  "type": "null"
                }
              ]
            },
            "gain": {
              "description": "Loudness of the clip's own soundtrack, if the host registered\none: 0 to 1 and above, times the gains of the groups above it.\n0 plays the picture silently. A normal numeric property:\nbindable and animatable.",
              "type": "number",
              "format": "double",
              "default": 1.0
            },
            "loop": {
              "description": "Repeat forever. Cannot be combined with `repeat`.",
              "type": "boolean",
              "default": false
            },
            "on_end": {
              "description": "Trigger fired when a play finishes (never for loops).",
              "type": [
                "string",
                "null"
              ],
              "default": null
            },
            "pick": {
              "description": "Which of several videos a play shows; ignored for one.",
              "$ref": "#/$defs/Pick",
              "default": "in_order"
            },
            "repeat": {
              "description": "Number of plays (fractions allowed); once when omitted.",
              "type": [
                "number",
                "null"
              ],
              "format": "double",
              "default": null
            },
            "rest": {
              "description": "Least seconds between one play starting and the next; a\ntrigger that comes sooner is dropped. 0 (default) never drops.",
              "type": "number",
              "format": "double",
              "default": 0.0
            },
            "retrigger": {
              "description": "What the trigger does while a clip is already showing:\n`restart` (default), `ignore` or `queue`. Not `overlap`: a\nlayer shows one picture at a time.",
              "$ref": "#/$defs/Retrigger",
              "default": "restart"
            },
            "size": {
              "description": "Drawn size `[width, height]`; the video's own when omitted.",
              "type": [
                "array",
                "null"
              ],
              "default": null,
              "items": {
                "type": "number",
                "format": "double"
              },
              "maxItems": 2,
              "minItems": 2
            },
            "stop": {
              "description": "Trigger name, or list of names, that stops it.",
              "$ref": "#/$defs/Triggers",
              "default": null
            },
            "trigger": {
              "description": "Trigger name, or list of names, that plays it.",
              "$ref": "#/$defs/Triggers",
              "default": null
            },
            "type": {
              "type": "string",
              "const": "video"
            },
            "video": {
              "$ref": "#/$defs/Choice"
            },
            "voices": {
              "description": "How many plays may wait their turn with `queue`. Default 4.",
              "type": "integer",
              "format": "uint32",
              "default": 4,
              "minimum": 0
            }
          },
          "required": [
            "type",
            "video"
          ]
        },
        {
          "description": "A sound the host registered under `sound` with its duration\n([`Engine::set_sound`](crate::Engine::set_sound)), played the way a\ntimeline is: on `trigger` or at load with `autoplay`, after `delay`,\nlooping or repeating, firing `on_end`. Draws nothing. What plays is\nreported by [`Engine::voices`](crate::Engine::voices); the engine\nnever touches samples.",
          "type": "object",
          "properties": {
            "autoplay": {
              "description": "Play when the show loads or the scene is entered.",
              "type": "boolean",
              "default": false
            },
            "bus": {
              "description": "Name of the bus the sound plays through; hosts route buses to\noutputs. [`MAIN_BUS`] when omitted, so every sound is on one.",
              "type": [
                "string",
                "null"
              ]
            },
            "delay": {
              "description": "Seconds between the trigger and the first sample.",
              "type": "number",
              "format": "double",
              "default": 0.0
            },
            "duck": {
              "description": "Step back while something else is sounding; see [`Duck`].",
              "anyOf": [
                {
                  "$ref": "#/$defs/Duck"
                },
                {
                  "type": "null"
                }
              ]
            },
            "gain": {
              "description": "Loudness, 0 to 1 and above, times the gains of the groups above\nit. A normal numeric property: bindable and animatable.",
              "type": "number",
              "format": "double",
              "default": 1.0
            },
            "loop": {
              "description": "Repeat forever. Cannot be combined with `repeat`.",
              "type": "boolean",
              "default": false
            },
            "on_end": {
              "description": "Trigger fired when a play finishes (never for loops).",
              "type": [
                "string",
                "null"
              ],
              "default": null
            },
            "pick": {
              "description": "Which of several sounds a play uses; ignored for one.",
              "$ref": "#/$defs/Pick",
              "default": "in_order"
            },
            "repeat": {
              "description": "Number of plays (fractions allowed); once when omitted.",
              "type": [
                "number",
                "null"
              ],
              "format": "double",
              "default": null
            },
            "rest": {
              "description": "Least seconds between one play starting and the next; a\ntrigger that comes sooner is dropped. 0 (default) never drops.",
              "type": "number",
              "format": "double",
              "default": 0.0
            },
            "retrigger": {
              "description": "What the trigger does while the sound is already playing.",
              "$ref": "#/$defs/Retrigger",
              "default": "restart"
            },
            "sound": {
              "$ref": "#/$defs/Choice"
            },
            "stop": {
              "description": "Trigger name, or list of names, that stops it.",
              "$ref": "#/$defs/Triggers",
              "default": null
            },
            "trigger": {
              "description": "Trigger name, or list of names, that plays it.",
              "$ref": "#/$defs/Triggers",
              "default": null
            },
            "type": {
              "type": "string",
              "const": "audio"
            },
            "voices": {
              "description": "With `overlap`, how many plays may sound at once; the oldest\nstops beyond it. Default 4.",
              "type": "integer",
              "format": "uint32",
              "default": 4,
              "minimum": 0
            }
          },
          "required": [
            "type",
            "sound"
          ]
        }
      ],
      "required": [
        "name"
      ]
    },
    "Model": {
      "description": "A physical model a transition follows instead of an ease.\n\nA lamp is not a fade. Much of how a panel of lamps looks is how they\nswitch, and a lamp switching is its filament's temperature chasing the\npower put into it: full brightness in a few tens of milliseconds, most\nof the light gone as fast when the power goes, then a dim red glow for\nmuch longer. A filament re-lit while still warm comes up quicker than\na cold one, which no duration and ease can say.",
      "oneOf": [
        {
          "description": "A glowing filament. The binding's value is the power put into it,\n0 to 1; a numeric property gets the light that comes out and a\n`tint` gets the filament's colour, which reddens as it cools.\n\nShaped by `kelvin`, `heating` and `cooling`, so it is any lamp\nwith a filament rather than one make of bulb.",
          "type": "string",
          "const": "incandescent"
        }
      ]
    },
    "NumberFormat": {
      "description": "Number to text conversion for text bindings.",
      "oneOf": [
        {
          "description": "Shortest form: `1500`, `2.5`.",
          "type": "string",
          "const": "plain"
        },
        {
          "description": "Rounded to an integer with comma thousands separators: `1,500`.",
          "type": "string",
          "const": "thousands"
        }
      ]
    },
    "Output": {
      "description": "How the finished frame reaches the display. Every field is optional: a\nscene's output overrides only the fields it sets, the rest come from\nthe show's, then from the defaults.",
      "type": "object",
      "properties": {
        "mode": {
          "description": "Color conversion; `rgb` by default.",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputMode"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "passes": {
          "description": "Effects applied to the finished frame as it is shown, in order. A\nscene's list replaces the show's; an empty list turns them off.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/Pass"
          }
        },
        "scaling": {
          "description": "How hosts scale the frame up to their surface; `smooth` by default.",
          "anyOf": [
            {
              "$ref": "#/$defs/Scaling"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "tint": {
          "description": "Color that full luminance maps to in the gray modes, `#RRGGBB`\n(white by default). Ignored in `rgb` mode.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "OutputMode": {
      "description": "How the finished frame's colors are converted for the display.",
      "oneOf": [
        {
          "description": "Full color, unchanged.",
          "type": "string",
          "const": "rgb"
        },
        {
          "description": "Luminance quantized to 4 levels (2 bits), times the tint.",
          "type": "string",
          "const": "gray2"
        },
        {
          "description": "Luminance quantized to 16 levels (4 bits), times the tint.",
          "type": "string",
          "const": "gray4"
        }
      ]
    },
    "Pass": {
      "description": "An effect on the finished frame, applied where it is shown (windows, the\nweb player), not by the offscreen renderer: it works at the surface's\nresolution, which a frame at canvas size does not have.",
      "oneOf": [
        {
          "description": "Every canvas pixel becomes a dot, as on a dot matrix display.",
          "type": "object",
          "properties": {
            "dots": {
              "$ref": "#/$defs/Dots"
            }
          },
          "additionalProperties": false,
          "required": [
            "dots"
          ]
        }
      ]
    },
    "PathData": {
      "description": "SVG path data: M L H V C S Q T A Z commands, absolute or relative.",
      "type": "string"
    },
    "Pick": {
      "description": "Which of a layer's several assets a play uses.\n\nEvery one of these is a function of how many times the layer has\nplayed, never of a running dice roll, so a show plays the same way\ntwice and a host that seeds the engine\n([`Engine::set_seed`](crate::Engine::set_seed)) decides how much it\nvaries between runs.",
      "oneOf": [
        {
          "description": "The next one each play, wrapping round at the end.",
          "type": "string",
          "const": "in_order"
        },
        {
          "description": "Any of them, which may be the one that just played.",
          "type": "string",
          "const": "random"
        },
        {
          "description": "All of them in a scrambled order, then scrambled again: varied,\nbut nothing is skipped and nothing repeats until the rest have had\ntheir turn.",
          "type": "string",
          "const": "shuffle"
        }
      ]
    },
    "Property": {
      "description": "An animatable layer property.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "x",
            "y",
            "opacity",
            "scale",
            "scale_x",
            "scale_y",
            "rotation",
            "tile_y"
          ]
        },
        {
          "description": "The text of a text layer; bindable, not animatable.",
          "type": "string",
          "const": "text"
        },
        {
          "description": "The font style of a text layer; bindable, not animatable.",
          "type": "string",
          "const": "font"
        },
        {
          "description": "The video a video layer plays; bindable, not animatable. Binding\nit lets one layer show whatever it is pointed at, rather than\nneeding a layer per clip.",
          "type": "string",
          "const": "video"
        },
        {
          "description": "The sound an audio layer plays; bindable, not animatable, and the\nmirror of `video`. Binding it lets one layer sound whatever it is\npointed at: a bed that follows the state a show is in, rather than\na layer per track each having to stop the others.",
          "type": "string",
          "const": "sound"
        },
        {
          "description": "Sprite sheet cell of an image layer: rounded down and clamped to\nthe sheet, so a linear key from 0 to n steps through n cells.",
          "type": "string",
          "const": "frame"
        },
        {
          "description": "Where a tiled image's pattern starts, along x and y, in the\nlayer's units. Animating one scrolls the texture under the layer.",
          "type": "string",
          "const": "tile_x"
        },
        {
          "description": "Loudness of an audio layer or a group's subtree.",
          "type": "string",
          "const": "gain"
        },
        {
          "description": "Whether the layer (and its subtree) shows and sounds; bindable,\nnot animatable. Bound, it is on when the binding's number is not 0.",
          "type": "string",
          "const": "visible"
        },
        {
          "description": "The color an image layer is multiplied by, as `#RRGGBB` or\n`#RRGGBBAA`; bindable, not animatable. An empty value leaves the\nimage alone.",
          "type": "string",
          "const": "tint"
        }
      ]
    },
    "Reel": {
      "description": "A row of cells carrying a ring of symbols, each rolling to the symbol\nthe layer's `text` asks of it, as the wheels of an odometer, a counter\nor a departure board do. Each cell stands somewhere on the ring of its\nown, so a change moves only the cells it reaches, and `stagger` keeps\nthem from moving in lockstep.\n\nA symbol is named by a character of `charset` and drawn either as that\ncharacter in a font style of the show, which needs no artwork and\nstays sharp at any size, or as the artwork `cells` gives it.",
      "type": "object",
      "properties": {
        "cells": {
          "description": "What each symbol looks like, one entry per character of `charset`.\nWithout it a symbol is drawn as its own character, in `font`.",
          "anyOf": [
            {
              "$ref": "#/$defs/ReelCells"
            },
            {
              "type": "null"
            }
          ]
        },
        "charset": {
          "description": "The characters naming the ring's symbols, in the order they pass\nby. A cell shows nothing for anything the text asks of it that the\nring does not carry.",
          "type": "string",
          "default": "0123456789"
        },
        "direction": {
          "description": "Which way round the ring a cell travels: `shortest` by default,\n`forward` for a wheel that only turns one way.",
          "anyOf": [
            {
              "$ref": "#/$defs/Direction"
            },
            {
              "type": "null"
            }
          ]
        },
        "duration": {
          "description": "Seconds one move takes; above 0.",
          "type": "number",
          "format": "double"
        },
        "ease": {
          "description": "How a step progresses; the same easings timelines know.",
          "$ref": "#/$defs/Easing",
          "default": "linear"
        },
        "font": {
          "description": "Font style from the show's `fonts` the symbols are drawn in as\ntheir own characters, when `cells` does not put artwork on the ring\ninstead.",
          "type": [
            "string",
            "null"
          ]
        },
        "offset": {
          "description": "Motion added on top of a move, in symbols: keys like a binding\ntransition's `offset`, so a cell can settle against its stop.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Key"
          }
        },
        "spin": {
          "description": "Trigger name, or list of names, that sets the row spinning: every\ncell travels its `turns` and lands on the symbol its text names at\nthat moment, whether or not that is the one it already shows.",
          "$ref": "#/$defs/Triggers",
          "default": null
        },
        "stagger": {
          "description": "Seconds each cell waits behind the one to its right, so a row does\nnot move as one piece. 0 by default.",
          "type": "number",
          "format": "double",
          "default": 0.0
        },
        "step": {
          "description": "How far a cell travels in one move, in symbols: one by default, so\nit lands on every symbol on the way, as a counter does. `null`\nmakes the whole journey one move instead, which is how a wheel\nspins: `duration` then covers all of it and `ease` shapes the spin\nrather than each symbol.\nAlways written back, `null` included: absent means one symbol at a\ntime, which is not what `null` means, so dropping it would change\na spinning wheel into a stepping one.",
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "default": 1.0
        },
        "turns": {
          "description": "Extra whole turns of the ring a cell makes before it lands, on top\nof the distance to its symbol. 0 by default; a spinning wheel takes\na few.",
          "type": "integer",
          "format": "uint32",
          "default": 0,
          "minimum": 0
        },
        "window": {
          "description": "How many symbols of the ring the cell shows at once, stacked with\nthe one it stands on in the middle. 1 by default; a wheel behind a\ntall window shows its neighbours as well.",
          "type": "integer",
          "format": "uint32",
          "default": 1,
          "minimum": 0
        }
      },
      "required": [
        "duration"
      ]
    },
    "ReelCells": {
      "description": "What a reel's symbols are drawn as. The charset stays the ring's\nidentity, so a show still says which symbol a cell lands on by its\ncharacter; this only says what that symbol looks like.",
      "oneOf": [
        {
          "description": "Vector artwork the host registered, by name, one per symbol.\nArtwork scales with the row, so a reel of pictures is as sharp as\none of letters.",
          "type": "object",
          "properties": {
            "vectors": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "additionalProperties": false,
          "required": [
            "vectors"
          ]
        },
        {
          "description": "Images the host registered, by name, one per symbol.",
          "type": "object",
          "properties": {
            "images": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "additionalProperties": false,
          "required": [
            "images"
          ]
        }
      ]
    },
    "Retrigger": {
      "description": "What an audio layer's trigger does while the layer is already playing.",
      "oneOf": [
        {
          "description": "Start over: the play so far stops.",
          "type": "string",
          "const": "restart"
        },
        {
          "description": "Start another play on top, up to the layer's `voices`.",
          "type": "string",
          "const": "overlap"
        },
        {
          "description": "Let the play finish; the trigger does nothing.",
          "type": "string",
          "const": "ignore"
        },
        {
          "description": "Let the play finish, then play: the trigger waits its turn, up to\n`voices` of them waiting at once.",
          "type": "string",
          "const": "queue"
        }
      ]
    },
    "Scaling": {
      "description": "How hosts should scale the rendered frame up to their surface.",
      "oneOf": [
        {
          "description": "Any factor, smoothly filtered.",
          "type": "string",
          "const": "smooth"
        },
        {
          "description": "Whole-number factors with nearest-neighbor sampling, so every\ncanvas pixel becomes a crisp square block (DMD-resolution content).",
          "type": "string",
          "const": "pixel_perfect"
        }
      ]
    },
    "Scene": {
      "description": "A switchable view of the show: its layers render only while it is the\nactive scene. Entering a scene (again) restarts it: timelines of the\nprevious scene stop, the entered scene's autoplay timelines start at 0.",
      "type": "object",
      "properties": {
        "layers": {
          "type": "array",
          "default": [],
          "items": {
            "$ref": "#/$defs/Layer"
          }
        },
        "name": {
          "type": "string"
        },
        "output": {
          "description": "Output color handling while this scene is active; the show's when\nomitted.",
          "anyOf": [
            {
              "$ref": "#/$defs/Output"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "trigger": {
          "description": "Trigger name, or list of names, that enters this scene.",
          "$ref": "#/$defs/Triggers",
          "default": null
        }
      },
      "required": [
        "name"
      ]
    },
    "SegmentStyle": {
      "description": "Segment layout of a segment display.",
      "oneOf": [
        {
          "description": "14 segments plus dot: letters and digits.",
          "type": "string",
          "const": "alpha14"
        },
        {
          "description": "7 segments plus dot: digits and `-`.",
          "type": "string",
          "const": "numeric7"
        }
      ]
    },
    "Shadow": {
      "description": "A copy of the text drawn behind it, offset by a few pixels: the\nordinary way to keep text legible over a moving picture.\n\nWhat is drawn is the text's whole silhouette, `border` included, in one\ncolor. A shadow of a bordered glyph is therefore the same shape as the\nglyph, which is what makes it read as a shadow rather than a second\noutline.",
      "type": "object",
      "properties": {
        "color": {
          "description": "`#RRGGBB` or `#RRGGBBAA`",
          "type": "string"
        },
        "offset": {
          "description": "How far behind the text it sits, `[x, y]` in canvas pixels. Down\nand to the right is positive; both may be negative.",
          "type": "array",
          "items": {
            "type": "number",
            "format": "double"
          },
          "maxItems": 2,
          "minItems": 2
        }
      },
      "required": [
        "color",
        "offset"
      ]
    },
    "Shape": {
      "description": "Vector shapes, in the layer's local coordinate space.\n\nOne of rect, circle or path; a rect may carry a corner radius beside it.",
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "radius": {
              "description": "Corner radius, clamped to half the shorter side, so a large one gives a pill. All four corners; absent is square.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "rect": {
              "description": "`[x, y, width, height]`",
              "type": "array",
              "items": {
                "type": "number",
                "format": "double"
              },
              "maxItems": 4,
              "minItems": 4
            }
          },
          "additionalProperties": false,
          "required": [
            "rect"
          ]
        },
        {
          "type": "object",
          "properties": {
            "circle": {
              "description": "`[cx, cy, radius]`",
              "type": "array",
              "items": {
                "type": "number",
                "format": "double"
              },
              "maxItems": 3,
              "minItems": 3
            }
          },
          "additionalProperties": false,
          "required": [
            "circle"
          ]
        },
        {
          "type": "object",
          "properties": {
            "path": {
              "$ref": "#/$defs/PathData"
            }
          },
          "additionalProperties": false,
          "required": [
            "path"
          ]
        }
      ]
    },
    "Sheet": {
      "description": "A sprite sheet layout: cells of `cell` `[width, height]` pixels,\n`columns` per row, numbered row by row from the top-left.",
      "type": "object",
      "properties": {
        "cell": {
          "type": "array",
          "items": {
            "type": "integer",
            "format": "uint32",
            "minimum": 0
          },
          "maxItems": 2,
          "minItems": 2
        },
        "columns": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "cell",
        "columns"
      ]
    },
    "ShowValue": {
      "description": "A value the show owns and animates, named and read like a variable.\n\nA timeline animates a property of its own layer, so two layers that\nmust move together have to duplicate its keys, and nothing then says\nthey are meant to agree: a scene restarting one, or an edit to one set\nof keys, parts them silently. A group shares a transform instead, but\nit scales positions along with everything else, so it only works when\nevery reader sits at the group's origin.\n\nA show value is the source both of them read. A host that sets a\nvariable of the same name takes it over, so a show can ship with its\nown motion that a host is free to seize.\n\nNothing in a show writes one. Variables are the host's inputs, and\ncontent writing them would make ownership ambiguous and allow a\nvariable that drives a timeline that writes that variable.",
      "type": "object",
      "properties": {
        "timelines": {
          "description": "What moves it, played exactly as a layer's timelines are. Several\nof them cover one value in stretches, each started by its own\ntrigger, the way a layer holds several timelines for one property.",
          "type": "array",
          "default": [],
          "items": {
            "$ref": "#/$defs/ValueTimeline"
          }
        }
      }
    },
    "Stop": {
      "description": "One color of a gradient, `at` a fraction of the way along it.",
      "type": "object",
      "properties": {
        "at": {
          "description": "0 at the start of the gradient, 1 at its end.",
          "type": "number",
          "format": "double"
        },
        "color": {
          "description": "`#RRGGBB` or `#RRGGBBAA`.",
          "type": "string"
        }
      },
      "required": [
        "at",
        "color"
      ]
    },
    "Stroke": {
      "description": "An outline drawn along a shape's edge, centered on it.",
      "type": "object",
      "properties": {
        "color": {
          "description": "`#RRGGBB` or `#RRGGBBAA`",
          "type": "string"
        },
        "width": {
          "description": "Line width in the layer's units, above 0; 1 by default.",
          "type": "number",
          "format": "double",
          "default": 1.0
        }
      },
      "required": [
        "color"
      ]
    },
    "Tile": {
      "description": "Repeat a layer's content across its `size` instead of stretching it\nto fit.\n\nA pattern otherwise has to be written out: a checkerboard is a hundred\nand twenty eight rectangles, and a reader cannot tell that from a\nhundred and twenty eight unrelated ones. The same want turns up for a\ngrid, a scanline overlay, a floor, a wall of dots, any texture meant to\ncover whatever it is put behind.\n\nTiling happens in the layer's own space, so a rotating or scaled group\ncarries the pattern with it rather than sliding underneath it.",
      "type": "object",
      "properties": {
        "offset": {
          "description": "Where the pattern starts, in the layer's units. Animate it and the\ntexture scrolls under a fixed window.",
          "type": "array",
          "default": [
            0.0,
            0.0
          ],
          "items": {
            "type": "number",
            "format": "double"
          },
          "maxItems": 2,
          "minItems": 2
        },
        "size": {
          "description": "Size of one tile in the layer's units. The content's own size when\nomitted, which is what \"repeat this at its natural size\" means.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "number",
            "format": "double"
          },
          "maxItems": 2,
          "minItems": 2
        }
      }
    },
    "Timeline": {
      "description": "A keyframed animation over one or more properties of its layer.\n\nA timeline runs when it is `autoplay` and the show loads, or when the\nhost fires its `trigger`. While running it owns the properties it\nanimates: timeline values override bindings, which override base values.",
      "type": "object",
      "properties": {
        "autoplay": {
          "type": "boolean",
          "default": false
        },
        "delay": {
          "description": "Seconds to wait after starting before the first key plays; the\ntimeline does not own its properties meanwhile. Loops and repeats\ndo not wait again.",
          "type": "number",
          "format": "double",
          "default": 0.0
        },
        "hold": {
          "description": "Keep the last value instead of giving the properties back.\n\nA timeline that ends hands its properties back to their binding or\nbase value, which is what a pulse or a flash wants. A fade *into* a\nstate wants to stay there, and writing the end value into the base\nas well only works while nothing else ever animates that property.\n\nHeld, a finished timeline keeps its properties at their last values\nuntil it is started again or its scene is left, and it ranks below\nany timeline still running, so a later flash on the same property\nwins while it plays and hands back to the held value afterwards. It\nmeans nothing on a loop, which never finishes.",
          "type": "boolean",
          "default": false
        },
        "loop": {
          "description": "Repeat forever. Cannot be combined with `repeat`.",
          "type": "boolean",
          "default": false
        },
        "name": {
          "type": "string"
        },
        "on_end": {
          "description": "Trigger fired when the timeline finishes (never for loops).",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "repeat": {
          "description": "Number of plays (fractions allowed: 2.5 stops halfway through the\nthird); once when omitted.",
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "default": null
        },
        "tracks": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Track"
          }
        },
        "trigger": {
          "description": "Trigger name, or list of names, that (re)starts this timeline.",
          "$ref": "#/$defs/Triggers",
          "default": null
        },
        "when": {
          "description": "A variable condition that (re)starts it on the rising edge, for\nhosts that send states rather than events. The edge belongs to the\nvariable, not to the scene: leaving a scene and coming back does\nnot replay it unless the condition turned true while away.",
          "anyOf": [
            {
              "$ref": "#/$defs/When"
            },
            {
              "type": "null"
            }
          ]
        },
        "while": {
          "description": "A variable condition it runs under: it plays while the condition\nholds and stops when it stops holding.\n\nWhat `when` cannot say. A blink that means \"this is lit\" should\nrun for as long as it is lit, and a looping timeline started on an\nedge would never stop. Unlike `when`, entering a scene starts it\nagain, since it describes a state the scene is in rather than\nsomething that happened.\n\nStopping is not finishing: it fires no `on_end`.",
          "anyOf": [
            {
              "$ref": "#/$defs/When"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "name",
        "tracks"
      ]
    },
    "Track": {
      "type": "object",
      "properties": {
        "keys": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Key"
          }
        },
        "property": {
          "$ref": "#/$defs/Property"
        }
      },
      "required": [
        "property",
        "keys"
      ]
    },
    "Transition": {
      "description": "How a bound property moves when its binding's value changes: from the\nvalue it has now to the new one over `duration` seconds.\n\nWhat is eased is the binding's output (after `map`, `scale` and\n`offset`), so it has to be a number: on a `text` binding numbers count\nup or down before they are formatted (whole numbers stay whole on the\nway), any other text jumps. A change while a transition runs starts a\nnew one from the value reached so far. When a show loads or a scene is\nentered, properties start at their value; nothing eases in.\n\nWith `wrap` the value lives on a ring of that size (360 for an angle,\n10 for a sheet with a frame per digit) and `direction` picks the way\nround.\n\nA transition is a small timeline played on every change. `duration`\nand `ease` say how a move progresses; `offset` adds keyed motion on top,\nin the value's own units, so it is the same size however far the move\ngoes (a reel settling against its stop); `step` plays a large change as\nseveral moves in a row.",
      "type": "object",
      "properties": {
        "cooling": {
          "description": "The same going the other way, and always the slower of the two: a\nfilament loses heat more slowly than the power puts it in. 0.06 by\ndefault.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "direction": {
          "description": "Which way round a wrapped value goes; needs `wrap`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Direction"
            },
            {
              "type": "null"
            }
          ]
        },
        "duration": {
          "description": "Seconds a change takes; above 0. Not used, and not needed, with a\n`model`, which decides its own timing.",
          "type": "number",
          "format": "double",
          "default": 0.0
        },
        "ease": {
          "$ref": "#/$defs/Easing",
          "default": "linear"
        },
        "heating": {
          "description": "How quickly the filament heats, in seconds: the time it takes to\nclose about two thirds of the gap to where it is heading. Full\nbrightness takes roughly five times this. 0.007 by default.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "kelvin": {
          "description": "Temperature of the filament at full power, in kelvin, which is\nalso the colour it glows there. 2700 by default, a warm white; a\nbigger lamp runs hotter and whiter, a small one cooler and redder.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "model": {
          "description": "Follow a physical model instead of easing over a duration.",
          "anyOf": [
            {
              "$ref": "#/$defs/Model"
            },
            {
              "type": "null"
            }
          ]
        },
        "offset": {
          "description": "Motion added on top of a move, along its direction of travel: keys\nlike a timeline track's, `t` in seconds from the start of the move,\n`v` in the value's units. Has to start and end at 0. A move lasts as\nlong as the longer of `duration` and these keys.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Key"
          }
        },
        "step": {
          "description": "Size of one move, above 0: a larger change plays as several moves in\na row, each with its own `duration`, `ease` and `offset`, the last\none shorter when the change is no whole number of steps.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "wrap": {
          "description": "Size of the ring the value lives on; above 0.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        }
      }
    },
    "Triggers": {
      "description": "A trigger name, or a list of names; firing any of them has the same effect.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        {
          "type": "null"
        }
      ]
    },
    "Value": {
      "description": "A value a host can push into the engine, and the type show variables hold.",
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "number",
          "format": "double"
        },
        {
          "type": "string"
        }
      ]
    },
    "ValueTimeline": {
      "description": "One stretch of a show value's motion: a timeline whose keys are the\nvalue itself, so it has no tracks and names no property.\n\nEverything above `keys` means what it means on a\n[`Timeline`](crate::Timeline), and the two are kept in step.",
      "type": "object",
      "properties": {
        "autoplay": {
          "description": "Start it when the show loads.",
          "type": "boolean",
          "default": false
        },
        "delay": {
          "description": "Seconds to wait after starting before the first key.",
          "type": "number",
          "format": "double",
          "default": 0.0
        },
        "hold": {
          "description": "Keep the last value instead of handing the value back.",
          "type": "boolean",
          "default": false
        },
        "keys": {
          "description": "Keyframes of the value itself.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Key"
          }
        },
        "loop": {
          "description": "Repeat for ever; cannot be combined with `repeat`.",
          "type": "boolean",
          "default": false
        },
        "name": {
          "type": "string"
        },
        "on_end": {
          "description": "Trigger fired when it finishes.",
          "type": [
            "string",
            "null"
          ]
        },
        "repeat": {
          "description": "Play it this many times; fractions stop part way.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "trigger": {
          "description": "What starts it; firing any of several has the same effect.",
          "$ref": "#/$defs/Triggers"
        }
      },
      "required": [
        "name",
        "keys"
      ]
    },
    "When": {
      "description": "A condition on a variable that starts something when it becomes true.\n\nA host that only sends states - lamps going on and off, a score\ncrossing a mark, a mode taking a value - has no trigger to fire, and\nwithout this every such host has to watch its own variables and invent\ntrigger names for them, which is show logic living outside the show.\n\nThe value is read the way a binding reads one: `map` replaces it when\nit lists it, then `threshold` turns a number into 0 or 1. True is any\nvalue that is not 0, and what starts the timeline is *becoming* true,\nso a lamp that stays on plays it once rather than every frame.",
      "type": "object",
      "properties": {
        "default": {
          "description": "Value for variable values `map` does not list.",
          "anyOf": [
            {
              "$ref": "#/$defs/Value"
            },
            {
              "type": "null"
            }
          ]
        },
        "map": {
          "description": "Replace the variable's value by looking it up here, as on a\nbinding: `{ \"multiball\": 1 }` is true exactly in that mode.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "$ref": "#/$defs/Value"
          }
        },
        "threshold": {
          "description": "A level: the value counts as true at or above it. Without one, any\nvalue that is not 0 is true.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "variable": {
          "type": "string"
        }
      },
      "required": [
        "variable"
      ]
    }
  }
}
